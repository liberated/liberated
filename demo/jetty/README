1. Create build/script/qxlib-noopt.js with "generate.py qxlib-noopt" in this
   directory.

2. Create build/index.html and build/script/rpcexample.js with "generate.py
   build" in this directory, and copy its build/index.html and
   build/script/liberated.demo.js to here.

3. Run ./server.sh

4. Connect via the browser to http://localhost:3000

5. Open the browser's debug console to watch RPC traffic
